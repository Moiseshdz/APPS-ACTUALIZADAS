name: ChatLAN Android v12 Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Preparando fuentes de ChatLAN Android v12"
